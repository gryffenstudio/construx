---
import Icon from './Icon.astro';

interface Props {
    icon: string;
    service: string;
    description: string;
}

const { icon, service, description } = Astro.props;
---

<div class='perspective-1000'>
    <div
        class='service-card initial-fold flex max-h-[343px] w-full max-w-[375px] flex-col items-center justify-center space-y-7 rounded-lg bg-white px-4 py-8 lg:py-12'
    >
        <Icon icon={icon} />
        <p class='text-center text-2xl font-bold capitalize'>{service}</p>
        <p class='text-center leading-8'>{description}</p>
    </div>
</div>
